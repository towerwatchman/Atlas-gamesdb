class config:
    def __init__(self) -> None:
        pass

    def user_readdonly():
        user = "u902432006_dbwriter"
        return user

    def password_readonly():
        password = "1Z#y!*Ki+"
        return password

    def host(type):
        if type == 1:
            host = "atlas-gamesdb.com"
        else:
            host = "localhost"
        return host

    def database():
        database = "games"
        return database
